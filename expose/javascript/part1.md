1. The result of adding `num1` and `num2` gets printed 
2. The result of adding `num1` and `num2` gets printed 
3. We should not use var because using var makes the variable accessable from outside of its block, which can cause errors and issues.
4. The result of adding `num1` and `num2` gets printed 
5. It will return an error because there is no `result` variable that is reachable in the current block.
6. It will print 0
7. It will return an error because there is no `result` variable that is reachable in the current block.